# Xuexibao frontend environment
# Copy this file to frontend/.env when you need to override the API address.

# Use the same origin in deployed builds. The beta gateway exposes FastAPI
# under /api and prevents the browser from ever seeing the backend port.
VITE_API_BASE_URL=/api
