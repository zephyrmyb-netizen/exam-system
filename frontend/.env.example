# Exam System 前端环境变量
# 复制此文件为 .env 并根据需要修改

# 后端 API 地址
# 默认自动推导：localhost / 127.0.0.1 时使用 http://127.0.0.1:8000
# 其他主机名时自动使用 http://<hostname>:8000
# 如需固定（如手机访问），请改为电脑局域网 IP，例如：
# VITE_API_BASE_URL=http://192.168.1.8:8000
VITE_API_BASE_URL=http://127.0.0.1:8000
