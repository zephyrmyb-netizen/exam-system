# 参考稿一致性迁移与导入恢复

- 执行时间：2026-07-14
- 执行窗口：Codex
- 任务目标：以 `exam-platform-ui` 交接稿为视觉与结构参考，补齐学习宝现有前端的页面层级和可用交互；保持后端接口与业务数据契约不变。
- 修改范围：`frontend/src/` 下首页、题库、课程练习、考试、AI 导入、个人中心、共享样式及对应前端测试；`frontend/src/data/releaseNotes.ts`；本留档和索引。
- 实际修改：
  - 首页改为参考稿的全宽学习头部、四项快捷入口、学习概览和最近题库结构；移除不在参考稿中的首页 AI 对话卡，AI 对话路由保留。
  - 题库页对齐“我的题库”筛选和图标化创建入口，保留真实课程管理接口。
  - 个人中心移除由题目数推导的虚假等级数据；连续学习和徽章在后端未提供真实数据时展示 `--`。
  - AI 导入任务恢复时识别远端已导入状态，恢复真实题目数和题库入口，展示服务端分块处理进度，避免已完成任务重新进入预览流程。
  - 考试页使用真实试卷题目提供底部答题卡；课程随机练习因接口不返回完整固定题序，未伪造答题卡或题目导航。
  - 课程练习答题态脱离设置页容器，避免移动端布局额外留白。
  - 移动端外壳按参考稿收紧至 420px，并将中间导入导航图标改为上传语义。
- 验证：
  - `npm.cmd run lint`
  - `npm.cmd run test -- --run`
  - `npm.cmd run build`
  - `git diff --check`
- Commit：`312ed20 feat: align learning UI with reference flows`；`ed6060d docs: archive reference parity migration`。
- Push：已推送至 `origin/codex/phase4-differentiation`。
- 遗留风险：参考稿包含静态示例数据。实现不伪造后端未返回的连续天数、徽章、完整随机练习题序或练习答题卡。
- 下一步：完成验证后提交并推送；真机复验首页、题库、导入、练习和考试的安全区与长文本截断。
