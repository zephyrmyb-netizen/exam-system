# 课程设计与毕业论文交付整理

- 执行时间：2026-09-17
- 执行窗口：Codex 主窗口
- 任务目标：检查当前项目质量，修复明确门禁问题，并补齐课程设计/毕业论文所需的架构、数据库、测试和写作材料。
- 允许修改的文件：工程质量配置、README、`docs/thesis/`、本操作留档；不改业务接口和数据库数据。
- 实际修改的文件：`frontend/eslint.config.js`、`frontend/package.json`、Prettier 标记的前端源码、`README.md`、`docs/thesis/*`、`docs/ops/INDEX.md`、本文件。
- 验证命令与结果：
  - `npm.cmd run lint`：通过。
  - `npm.cmd run test -- --run`：60 个测试文件、309 项测试通过。
  - `npm.cmd run build`：通过。
  - `backend\\.venv\\Scripts\\python.exe -m ruff check backend`：通过。
  - `backend\\.venv\\Scripts\\python.exe -m pytest backend\\tests -q`：444 通过、1 跳过。
  - `backend\\.venv\\Scripts\\python.exe scripts\\security_check.py`：3/3 通过。
  - `npm.cmd run format:check`：整改前失败（38 个文件），格式化后复验通过。
  - 格式化后再次执行 `npm.cmd run lint`：通过，无警告。
  - 格式化后再次执行 `npm.cmd run test -- --run`：60 个测试文件、309 项测试通过。
  - 格式化后再次执行 `npm.cmd run build`：通过。
- Commit：未执行；工作区包含其他窗口的大量未提交改动，禁止混合提交。
- Push：未执行。
- 遗留风险：当前分支领先远端且工作区包含大量跨模块改动；pytest 有 TestClient 弃用与开发配置警告；真实 AI 与真机路径尚需人工验收。
- 下一步：完成最终门禁；按维护路线图先拆分并提交现有改动，再进行 AI 导入和页面大文件重构。
