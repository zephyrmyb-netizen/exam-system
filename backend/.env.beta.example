# This file is documentation only. Do not commit a real token or secret.
# start-beta.ps1 reads backend/.env for SECRET_KEY / INVITE_CODE, then overrides
# APP_ENV and DATABASE_URL for a separate local test database.
SECRET_KEY=replace-with-a-long-random-value
INVITE_CODE=replace-with-a-private-test-invite-code
